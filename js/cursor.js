import { rainbowCursor } from "cursor-effects";
new rainbowCursor({
    length: 100,
    colors: ["rgba(33, 255, 255,0.5)", "rgba(255, 33, 133,0.5)"],
    size: 4,
    // element: cursorEffectElement
  });